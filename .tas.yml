---
framework: jest
preRun:
     command:
        - npm ci
preMerge:
  pattern:
    - "./**/unit/**/*.spec.js"
postMerge:
  pattern: 
    - "./**/unit/**/*.spec.js"
postRun:
     command:
        - node --version
version: 1.0
